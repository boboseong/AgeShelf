# AgeShelf — 연령별(한 살 단위) 유아 도서 순위 프로젝트 계획

> 목적: 도서관정보나루 공공도서관 대출 데이터를 이용해 "0~5세 영유아"라는 큰 묶음이 아니라
> **0세, 1세, 2세 … 한 살 단위**로 인기 도서와 **연령 적합도(상대적 선택)**를 산출한다.
> 1차 용도는 24개월 아이의 대출 목록, 2차 용도는 결과를 정리한 공개 사이트.

---

## 1. 매뉴얼에서 확인한 핵심 사실 (v20260210 API / v20260825 MCP)

### 1-1. 이 프로젝트를 가능하게 하는 것
| 항목 | 내용 | 근거 |
|---|---|---|
| **한 살 단위 조회** | `loanItemSrch`(인기대출도서)에 `from_age`/`to_age` 파라미터가 있어 `from_age=2&to_age=2` 처럼 **단일 연령** 조회 가능. 연령 코드(`age=0` = 0~5세 묶음)와 별개. | API p.10-11 |
| **집계 규모** | 인기대출도서는 ISBN 기준, 조건당 **최대 5,000건**. `pageSize` 조절 가능(예시 300). | API p.9, 12 |
| **필터** | 기간(`startDt/endDt`), 성별, 지역/세부지역, **ISBN 부가기호(`addCode=7`=아동)**, KDC 대/세부주제. 다중선택은 `;` 구분. | API p.10 |
| **응답 필드** | ranking, bookname, authors, publisher, publication_year, isbn13, addition_symbol, vol, class_no/class_nm, bookImageURL, bookDtlUrl, **loan_count** | API p.11 |
| **기간 품질** | "최소 3개월 이상"의 대출기간을 넣어야 양질 데이터. 미입력 시 startDt=2014-01-01. | API p.30 |
| **호출 한도** | 마이페이지에 호출서버 IP 등록 시 **1일 30,000건**(IP 최대 3개). API+MCP 통합 집계. | MCP p.3 |
| **JSON** | 모든 엔드포인트 `format=json` 지원. | 공통 |

### 1-2. 보조로 쓸 수 있는 것
| 엔드포인트 | 용도 | 한계 |
|---|---|---|
| `srchDtlList?loaninfoYN=Y&displayInfo=age` | 책 1권의 연령대별 순위/대출건수 + 책소개(description) | 연령대가 **영유아/유아/초등…** 코드 단위, 최근 90일, 조건별 상위 1,000위까지만 → 프로필 계산엔 부적합, 검증/보강용 |
| `usageAnalysisList` | 12개월 월별 대출추이, 다대출 그룹(연령×성별), 키워드 50개, 함께 빌린 책 10권, 마니아/다독자 추천 | 그룹이 역시 코드 단위. 책 상세 페이지 보강용 |
| `extends/loanItemSrchByLib` | 도서관 1곳의 이용자그룹별(전체/영유아/유아/초등/청소년/성인) 상위 20권 | 20권, 30일 → 참고용 |
| `libSrchByBook`, `bookExist` | 특정 ISBN 소장 도서관 / 대출 가능 여부 | 사이트의 "우리 동네에서 빌리기" 기능 |
| `recommandList`(mania/reader) | ISBN 기반 추천 | 사이트 보강용 |
| `readQt` | 지역·연령별 독서량/독서율 | 통계 페이지용 |

### 1-3. 코드표 요점
- 연령 코드: `0`=영유아(0~5), `6`=유아(6~7), `8`=초등(8~13), `14`=청소년, `20/30/40/50/60`, `-1`=미상, `a8/a10/a12/a14/a17`=학년 세분.
  → **0~5세 코드 하나로는 24개월 아이용 선별 불가** (사용자 문제의식과 일치).
- ISBN 부가기호(`addition_symbol`, 5자리): 1째 자리 **7=아동**, 2째 자리 **7=그림책/만화**, 3~5째 자리 = 내용분류.
  → `addition_symbol` 이 `77xxx` 이면 **아동 그림책**. 그림책 전용 필터에 사용.
- 지역 코드: 서울 11, 경기 31 … 세부지역 5자리(예: 강남구 11230).

### 1-4. MCP vs REST API
- MCP `get_popular_loans` 도 `from_age/to_age`를 지원하지만, MCP는 같은 데이터를 AI 에이전트가 자연어로 쓰는 **래퍼**다.
- 수천 건을 반복 수집하는 작업은 LLM 컨텍스트를 낭비하므로 **수집·계산은 REST API(Python)**, MCP는 Claude Desktop에서의 **탐색/검증(예: "2세가 많이 빌린 책 보여줘")** 용도로만 쓴다.
- MCP 설정(Claude Desktop `claude_desktop_config.json`):
  `npx -y mcp-remote https://www.data4library.kr/mcp --header "X-Auth-Key:<키>"`

---

## 1-5. 실데이터로 확인한 사실 (2026-09-09 스모크 테스트, 키 승인 후)
| 확인 | 결과 | 영향 |
|---|---|---|
| 단일 연령 조회 | `from_age=a&to_age=a` 동작. 상위 100권 Jaccard: 1세vs2세 0.09, 2세vs4세 0.12, 4세vs7세 0.02 | **연령 해상도 확보** (프로젝트 전제 성립) |
| 연령의 의미 | 2016년 슬라이스의 2세·4세에 당시 유아책(안돼 데이빗, 수박 수영장)이 나옴 | **대출 당시 나이** → 전체 기간 분석 유효 |
| pageSize | 5,000 한 번에 반환. 상한은 **행** 기준 | 쿼리당 1회 호출 |
| ISBN 중복 행 | 같은 ISBN 이 `vol` 표기 차이(예: '10' vs '')로 2행 이상. 5,000행 = 고유 4,280권 | 쿼리 안에서 **ISBN 별 합산** 필수 (build_metrics 1단계) |
| 무분할 커버리지 | 2세 12개월: 전국 상위 5,000행 = 4,280권·대출 59.7k. 지역 17개 분할 합집합 = 28,667권·101k | 지역 분할이 커버리지 7배였으나 **사용자 결정으로 전국 무분할만 사용**. 결측은 상한 U·비관적 lift 로 처리 |
| 지역 가산성 | 상위권은 지역합 ≈ 전국(107 vs 102, 3~5% 결손: 지역 미상 도서관 추정) | 비율 지표엔 영향 미미 |
| addCode | 1자리(7)만 허용, 77/77810 은 0행 | 그림책 필터는 응답의 `addition_symbol` 후처리 |
| 코호트 규모(12개월, 상위5000 합) | 0세 26k · 1세 37k · 2세 60k · 3세 114k · 4세 230k · 5세 393k · 6세 596k · 7세 1.03M · 8~10세 1.4~1.5M · 13세 303k | 어린 나이일수록 표본 작음 → lift 에 shrinkage 필수 |
| **0·1세 오염** | 0세·1세 상위가 초등 고학년 책(5번 레인, 긴긴밤, 악플 전쟁, 탄소 중립이 뭐예요?) | 생년 미입력 카드가 0세로 등록되는 등의 **데이터 오류 코호트**로 추정. 0·1세 페이지는 lift 기반만 신뢰하고 "많이 빌린 책"은 lift<1 제외 또는 비노출 |
| 2세 신호 | 상위: 그레이트북스 베베 시리즈(변기에 쉬해요, 놀이터는 재미있어요…) + 초등 동화 일부 혼입 | 24개월용 신호 존재. lift 로 혼입 제거 |
| 가족 카드 오염 | 2세 상위 100 중 비아동 2권(부가기호 없음) | addCode=7 로 대부분 해결 |

## 2. 방법론 — "상대적 선택"으로 추천 연령을 정하는 법

### 2-1. 데이터 행렬과 5,000건 컷오프 처리
- 연령 a ∈ {0..8}, 기간 P(기본: 전체 2014~). 각 a에 대해 `loanItemSrch?from_age=a&to_age=a&addCode=7` 를 5,000건까지 수집.
- **어떤 나이의 상위 5,000엔 있고 다른 나이엔 없는 책** 문제는 두 겹으로 다룬다.
  1. **결측을 줄인다 — 분할 수집.** 성별(`gender=0/1/2`, 대출자 속성이라 합산)과 KDC 셀(대주제 0~7,9 + 문학 세부 80~89, 도서 속성이라 같은 값의 재관측 → 합집합)로 쪼개면 나이당 커버리지가 5,000 → 최대 3×20×5,000. 셀이 5,000 미만이면 그 셀은 **완전 관측**이라 "없음 = 0건"이 확정된다.
  2. **남은 결측은 구간값으로 다룬다.** 캡된 셀에 없는 책은 `[0, cutoff)` (cutoff = 그 셀 5,000위의 대출수). 책마다 하한 L(관측)과 상한 U(= L + 미관측 캡셀들의 cutoff 합)를 유지한다.
- `cells.parquet` 에 셀별 n / capped / cutoff 를 기록하고 `build_metrics.py` 가 U 를 계산한다.

### 2-2. 지표
| 지표 | 정의 | 의미 |
|---|---|---|
| `T[a]` | Σ_book L[book,a] | 연령 a 대출 규모(코호트 보정용) |
| `profile[b,a]` | ((L+U)/2)[b,a]/T[a] 를 책별 정규화 | 연령 분포(표시용, 합 1). 미관측 나이는 흐리게 표시 |
| `lift_pess[b,a]` | 나이 a 는 **L**(하한), 나머지 나이 중 **1~8세는 U**(상한), **9세 이상은 관측값(미관측 0)** 으로 계산한 P(책\|나이)/P(책), 가상표본 m=30 shrinkage | **비관적 lift**. 결측 때문에 "그 나이 특화"로 보이는 착시를 차단하되, 보충 수집이 없어 상한이 너무 큰 9세 이상은 비관값을 쓰지 않음 (사용자 결정 2026-09-09) |
| `fit_score[b,a]` | log1p(L[b,a]) × clip(lift_pess, 0.5, 3) | 인기 + 적합도 종합 |
| `peak_age` | argmax profile | 가장 많이 선택받은 나이 |
| `mean_age`, `spread` | profile 가중 평균·표준편차 | 넓으면 전연령 스테디셀러 |
| **`skew`** | profile 표준화 3차 모멘트 | **편포 방향**. >0 큰 나이 쪽 꼬리, <0 어린 쪽 꼬리 |
| **`band`** | profile 질량 **70%를 담는 최단 연속 구간** | 추천 연령. 대칭 규칙이 아니라 꼬리 방향으로 자연히 늘어남 (예: 최고점 2세, 밴드 2~4세) |
| **`shape`** | 최고점 위/아래 질량 비교 | "더 큰 아이 쪽으로 넓음" / "더 어린 아이부터 봄" / "대칭" 텍스트 |
| fit 후보 조건 | 그 나이에서 관측 & 관측 나이 ≥ 2 & 그 나이 하위 30% 제외 | 희귀본·미관측 배제 |

합성 데이터 검증(`scripts/make_sample.py`, 나이당 120권만 관측되는 가혹한 컷오프): 최고점 ±1세 이내 100%, 왜도 부호 일치 100%, 관측 2개 이하 도서의 max_lift 중앙값 1.25 vs 충분관측 1.71 로 억제 확인.

### 2-3. 사이트에서 보여줄 두 축 (연령 a 페이지마다)
1. **"a세가 가장 많이 빌린 책"** — popularity 순 (기존 랭킹과 비교 가능).
2. **"a세에 딱 맞는 책"** — fit_score 순, 단 L[b,a]가 그 연령 상위 30% 이상인 책만(희귀본 배제).
3. 책 카드마다 0~8세 **연령 프로필 막대**, peak age, 추천 밴드, 표지, 출판사, 정보나루 상세 링크.

### 2-4. 오염(contamination) 대응
| 오염원 | 현상 | 대응 |
|---|---|---|
| 아이 카드로 성인 책 대출 (가족 카드 돌려쓰기) | "1세 인기도서"에 성인 소설이 섞임 | 수집 시 `addCode=7`(아동) 필터 + 후처리로 `addition_symbol[0]=='7'` 확인. 비교용으로 무필터 수집도 1회 수행 |
| 부모 카드로 아동 책 대출 | 아동서가 30·40대 랭킹에 등장, 아이 연령 정보 소실 | 연령 프로필 계산에서는 0~8세 카드 대출만 사용. 별도로 `age=30;40&addCode=7` 수집 → "부모가 고른 아동서" 보조 랭킹(전체 인기도 신호) |
| 형제 카드 혼용 | 4세 형 카드로 1세 동생 책 대출 | 구조적으로 분리 불가 → 한계로 명시. lift 기반이라 부분적으로 완화됨 |
| 5,000건 컷오프 | 롱테일 결측 | 분할 수집으로 결측 축소 + 상한 U 기반 비관적 lift + 미관측 표시(2-1, 2-2) |
| 연령 정의 불명 (만 나이 vs 연 나이) | 경계가 1년 흔들릴 수 있음 | 알려진 책(예: 《달님 안녕》《사과가 쿵!》 0~2세, 《구름빵》 4~6세)의 peak 로 추정, 사이트에 "정보나루 연령 기준" 주석 |

### 2-6. 실데이터 반영 후 확정된 오염 처리 (2026-09-09)
| 코호트 | 관찰 | 처리 |
|---|---|---|
| **0세** | 전체기간 11.8만 건. 상위가 온책읽기·교과연계 도서(깊은 밤 필통 안에서, 긴긴밤, 만복이네 떡집, 악플 전쟁). 8~13세 풀 비례 차감으로도 안 빠짐 → **생년 없는 학급·기관 단체대출**로 추정 | 지표에서 **제외**(`DROP_AGES`). 사이트는 1세 코호트를 "0~1세"로 표기. 1세 상위가 눈 코 입·엄마랑 뽀뽀·사과가 쿵!·달님 안녕 등 아기 그림책이라 실질적으로 0~1세 목록 역할 |
| 1~3세 | 잡음 비율 추정 1세 6%, 2세 3%, 3세 2% | (a) **게이트**: 4~13세 프로필의 85%+가 8~13세인 '명백한 초등 책' 1,875권의 1~3세 대출은 통째로 제외. (b) 남은 배경은 8~13세 풀 분포에 비례해 차감 (`denoise()`) |
| 검증 | 긴긴밤 peak 0세 → **13세(밴드 10~13세)** 로 교정. 달님 안녕·사과가 쿵!·두드려 보아요 밴드 1~2세, 구름빵 3세(1~5세), 흔한남매 8세(6~11세), 2세 fit 상위: 덤프차가 꽈당!·괜찮아·두드려 보아요·누구 엉덩이?·아가야 밥 먹자·빠이빠이 기저귀! | 방법론 실데이터 통과 |

### 2-7. 분야별 보충 수집 반영 후 조정 (2026-09-09)
| 항목 | 결과·조정 |
|---|---|
| 커버리지 | 도서 15,175 → **102,010권**. 1~8세 관측률 29% → 60~67%, 관측 연령 수 중앙값 3 → 6. 2세 코호트 총량 57만 → 126만 건 |
| 9~13세 | 보충 수집 범위 밖이라 상위 5,000행만 있음(관측률 4%). 이 나이의 느슨한 상한이 유아 책 lift 를 깎고 초등 게이트를 과대 판정(23,317권)하는 부작용 → **미관측 칸은 0으로 두고**(중간값 채움 폐지), **상한 U 는 1~8세(`BOUND_AGES`)에만 반영**, 게이트·잡음 모형은 8~13세 관측값만 사용. 조정 후 게이트 4,779권, 잡음 비율 1세 7%·2세 5%·3세 5% |
| 목록 변화 | 1세 TOP30 29/30 유지, 2세 22/30 유지(사과가 쿵!·코~ 자자·머리 감는 책 등 8권 신규 진입). 3~5세는 보충 수집으로 관측된 전집·시리즈(그레이트북스 생활동화, 옥토넛, 책읽는곰 탈것)가 다수 진입 |
| 시리즈 홍수 | 같은 전집 30여 권이 목록을 채우는 문제 → 사이트·CSV 목록에서 **시리즈 키(ISBN 9자리 접두+출판년+부가기호)당 최대 3권** (`diversify`). 순위 자체는 유지 |
| 검색 | 총대출 상위 30,000권 색인(약 5MB), 나머지는 상세 페이지 대신 정보나루 링크 |

### 2-8. 점수 v3 (2026-09-09 확정) — lift 대신 "분포 중심도"
lift(P(책|나이)/P(책))는 분모가 1~13세 전체라 아기 책이면 1~3세 어디서나 커지는 구조적 문제가 있었다(눈 코 입 2세 lift 16). 실험(v1: log(대출)×중심도×집중도) 결과 집중도가 전집 권들을 과대평가해 4세 상위 20권 중 19권이 한 전집이 되는 문제도 확인 → v3 채택.

| 요소 | 정의 | 비고 |
|---|---|---|
| 정규화 | W[b,a] = L[b,a] / T[a]^γ, **γ=1 (lift 기준)** | 사용자 결정 2026-09-10. γ=0.75가 통념 권장연령 25권 대비 오차 0.53세(γ=1은 0.71세)로 근소하게 더 맞지만, "그 나이 아이 한 명당 선택률"이라는 정의의 명료성을 택함. 밴드가 0.75 대비 0.3~0.8세 어려짐 |
| 스무딩 | 같은 발행형태(그림책/전집/단행본) 평균 분포 쪽으로 **κ=150건** | 소량 도서 최고 비중 0.29 → 0.23 (대형 도서 0.20) |
| **중심도** c | 1 − 2·\|F_mid − 0.5\| (구간 중앙 누적비율). 비대칭: 지난 책(F>0.5) 그대로, 앞으로 볼 책(F<0.5)은 지수 1.5로 완만 | 중앙 1, 꼬리 0. "1~7세 고른 책은 3~4세가 중심" 반영 |
| 집중도 s | min(3, 13·P) | **점수에 넣지 않음**(전집 과대평가). 카드 배지 "이 나이에 집중"(s≥2) |
| 자격 | c ≥ **0.3** & 그 나이 대출 ≥ 30 | 사용자 결정 2026-09-10 (0.4 → 0.3). 가장자리·희귀본 배제 |
| **점수** | fit = L[b,a] × c[b,a] | log 를 쓰지 않음(인기 과압축 → 전집 홍수) |
| 밴드 | P 의 **25~75% 분위**(중앙 50% 질량), 중앙 나이 = 50% 분위(보간) | 20~80 은 통념보다 넓음(사과가 쿵 1~4세), 25~75 는 1~3세로 근접 |
| lift | 참고값으로 유지(상세 페이지 표) | |

효과: 눈 코 입 2세 10위권 → 167위(중심도 0.58), 4세 상위 = 수박 수영장·장수탕 선녀님·알사탕·빵 공장이 들썩들썩·왜냐면…, 흔한남매는 7세부터 자격.

### 2-9. 태그와 필터 (2026-09-10 확정)
| 태그 | 정의 | 규모(전체 101,790권) |
|---|---|---|
| 분야 | KDC 2자리 **세부 라벨**(부모용 이름, 예: 81 한국 창작, 37 유아교육·생활습관, 45 지구·공룡). 대분류로 묶어 표시 | 세부 라벨 ~90개 |
| 형태 | ISBN 부가기호 2번째 자리: 그림책 / 전집 / 단행본 / 도감 / 기타 | 전집 58k, 그림책 25k, 단행본 16k |
| 출판사 | 괄호 안 한글명 우선·공백 제거로 정규화. 나이별 후보 수 상위 40개를 facet 으로 | |
| 성격 | 먼저 보기 좋은(43.6k) / 커서도 보는(47.6k) / 여러 나이 스테디(폭≥2.5세 & 대출 상위 10%, 1.7k) / 10년 스테디셀러(2015년 이전 & 상위 10%, 4.2k) / 베스트셀러(상위 1%, 1.0k) | |
| 요즘 인기 | 최근 12개월 전국 슬라이스(`loans_long_recent`, 14회)에서 1~8세 대출 합 상위 10% (≥965건) | 1,046권 |

풀: 나이마다 후보(자격 통과) 중 전체 상위 900 + **세부 라벨·형태·출판사·성격마다 상위 30권**을 합집합 → `public/data/pool/{age}.json`(1.1~1.4MB, 필터 적용 시에만 로드). 필터는 그룹 안 OR, 그룹 간 AND, 결과는 추천도순(인기 탭은 대출순), 시리즈당 3권. 카드 태그 칩 클릭·상세 페이지 태그 링크로도 필터 진입. 검색 페이지는 대분류·형태·성격 칩.
주제 태그(제목 기반 18개)는 제안만 하고 미구현 — 키워드 API 보강과 함께 검토.

### 2-10. 목록 구조 v2: 얇은 색인 + 클라이언트 렌더링 (2026-09-10 확정)
사용자 지적: "후보 29,488권"인데 보이는 건 300권(+필터 풀 3,000권). 상한 세 겹(HTML 300권, 풀 3,000권, 표시 120권)이 원인.
| 구성 | 이전 | 지금 |
|---|---|---|
| 나이 페이지 HTML | 카드 600장(8만 px) | 첫 화면 60권만 서버 렌더(`TEASER_N`), 이후 색인으로 클라이언트가 그림 |
| 필터·정렬 대상 | 풀 ~3,000권(facet별 상위 30) | **후보 전체 + 인기순 상위 3,000** (`public/data/index/{age}.json`, 행당 ~200B: 제목·저자·출판사id·표지(접두 압축)·밴드·중앙나이·형태·kdc2·성격비트·추천도·인기순위·위치·대출·시리즈id·비중 13자 양자화) |
| 표시 | 120권 고정 | 100권씩 "더 보기" + 스크롤 자동 로딩, 끝까지 |
| 검색 색인 | 총대출 상위 30,000권 | **전체 101,790권**(압축 행, 표지 없음) |
| 도서관 필터 | 핵심 1,212권 소장 정보 ∩ 풀 | 동일(소장 정보 범위가 한계) |
안내 문구는 "후보 N권 중 M권 · 정렬 기준"으로 실제 수와 일치.
**시리즈당 권수 제한 해제**(2026-09-10 사용자 결정): 2-7의 시리즈당 3권 규칙을 서버(첫 화면·상세 대상·CSV)와 클라이언트 모두에서 없앰. 전집 권들이 연속으로 나올 수 있음.

### 2-11. 상세 페이지 완전 동적화 (2026-09-10 확정)
정적 상세(`/book/{isbn}/`, 상위 1,314권)를 없애고 `/b/?isbn=…` 하나로 통일. 조각 `data/books/NNN.json`(ISBN 끝 3자리, 1,000개 × ~100KB)에서 읽어 그린다.
얻는 것: 템플릿 한 벌, 빌드 11페이지, 어떤 책이든 같은 주소. 잃는 것: 링크 미리보기·검색 엔진 노출(제목이 스크립트 실행 후에만 생김). 필요해지면 상위권만 정적으로 다시 얹는다.

### 2-12. 추천도 v3.1: 100점 덧셈형 (2026-09-10 확정)
문제: 대출수 × 위치는 대출수(30~4,500건, 150배)가 위치(0.3~1, 3배)를 압도해 4세 "딱 맞는 책" TOP30 이 인기 TOP30 과 26권 겹침. 상세 페이지 점수도 나이별 코호트 크기에 따라 달라 비교 불가.
| 항목 | 정의 |
|---|---|
| 위치 점수 | 50 × 위치(중심도) |
| 인기 점수 | 50 × (log L − log 30) ÷ (log Lmax_a − log 30) — 자격 최소 대출 30건을 0점, 그 나이 후보 최대를 만점으로 (2026-09-11 하한 0 조정: 상위 1~100위 격차 5.5→8.7점) |
| **추천도** | 위치 점수 + 인기 점수 (0~100). 순위와 표시 값이 같음. 개월 수 페이지는 두 나이의 추천도를 보간 |
후보 비교(4세): 인기 TOP30 겹침 26 → 8, 상위 = 마술 연필·왜냐면…·기차가 덜컹덜컹·구름빵. 수박 수영장(중앙 5.9세)은 딱 맞는 책 112위, 많이 빌린 책 1위. 백분위 방식(C)은 인기를 너무 눌러 구름빵 전집이 상위를 채워 기각. 가중치 50:50 은 사용자 결정.

### 2-13. 적합도 v3.2: 분위 × 밀도 기하평균 (2026-09-11 확정)
문제: 분위 기반은 책 안의 상대 순위만 보므로 **넓게 통하는 책일수록 각 나이 비중이 작아 백분위가 빨리 이동 → 하단이 더 깎이는 역설**. (딩동 거미: 1~7세 고르게인데 2세 적합도 50%)
표준편차(z) 방식 4종을 실측 비교 → 통념 권장연령 일치가 0.63 → 0.46~0.53 으로 떨어지고 구간이 6~7년으로 늘어져 기각. 정규분포 가정이 실제 비대칭 분포와 안 맞고, 흔한남매가 2세 15% 까지 새어듦.
**채택: c = √(분위 × 밀도)**, 밀도 = P[b,a] / max_a P[b,a].
| 검증 | 현재(분위) | 밀도 | 기하평균(채택) |
|---|---|---|---|
| 통념 일치(자카드) | 0.63 | 0.53 | **0.64** |
| 50%+ 구간 폭 | 3.9세 | 5.0세 | 4.2세 |
효과: 넓은 책 하단↑(딩동 거미 2세 50→61%, 수박 수영장 2세 39→60%), 뾰족한 책 주변↓(국립공원 2세 18→9%, 4세 28→10%), 타연령 차단 유지(흔한남매 2세 3→2%). 문턱 30% 유지 시 후보 2세 12,631→12,819, 4세 29,488→28,610 로 거의 동일.

### 2-14. 주제 태그 + 원문 키워드 (2026-09-12 확정)
사용자 결정: 주제 27개 그대로, 필터는 주제군으로, 원문 키워드는 검색·상세에 통째로.
- 원천: 정보나루 `keywordList`(책소개·서평 TF-IDF, 가중치). 1~7세 추천도 상위 3,000권 합집합 11,773권 조회 → 8,868권(75%)에 키워드, 빈 책 2,905권 중 **전집 2,334권**(전집의 44%; 방문판매 전집은 책소개 미등록).
  파일럿(89권): 인기 구간과 무관하게 86~97% 커버, 평균 45개. 잡음: 저자·출판사·수상 문구·범용어(그림책 65%, 아이들 44%…).
- 주제 27개(동물/곤충·벌레/공룡/탈것/우주·지구/자연·계절/몸·건강/배변·기저귀/잠자리/씻기·습관/음식/가족/친구·우정/감정·마음/한글·말놀이/숫자·색·모양/유치원·학교/상상·모험/옛이야기·명작/놀이·장난감/예술·음악/과학·탐구/직업·사회/역사·인물/성장·자립/안전·예방/캐릭터·TV).
  키워드 단어를 사전과 **정확 일치**(부분 일치는 '이'·'배' 등이 과다 매칭), 범용어는 0.4배, 문턱 = 가중치 4+ & 단어 2개 / 가중치 8+ / 가중치 3+ & 비중 8%+. 상위 3개.
  초안에서 뺀 것: '화가'(painter ↔ 화남), '마을·동네'(직업·사회 오염). 더한 것: 달님→잠자리, 수영장→놀이.
- 보완: 키워드 없는 책은 제목 단어 → KDC 2자리. 전체 101,790권 중 79% 부여(키워드 7,884 / 제목 49,602 / KDC 23,043).
- 화면: 나이 페이지 '주제' 필터(주제 비트, AND), 카드 주제 칩 2개, 검색은 키워드 문자열까지 포함해 매칭(키워드로만 맞으면 '키워드 일치: …' 표시), 상세는 원문 키워드 전체를 가중치 순으로(클릭 → 검색).
- 확장: 상위 5,000권(17,451권)은 일일 한도(30,000) 여유 시 `collect_keywords.py --top 5000`.

### 2-5. 검증
- 코드 `age=0`(0~5세) 랭킹과 한 살 단위 결과를 나란히 놓고 차이를 보여준다(프로젝트 동기 입증).
- 연도별(2023/2024/2025)로 같은 계산을 해 peak_age 안정성 확인.
- 출판사 표기 권장연령이 있는 책 30권 정도로 수동 대조.

---

## 3. 파이프라인 / 기술 스택

```
AgeShelf/
├─ .env                      # DATA4LIBRARY_KEY (git 제외)
├─ PLAN.md
├─ scripts/
│  ├─ naru.py               # API 클라이언트 (키 로드, 재시도, JSON, 캐시)
│  ├─ smoke_test.py         # 키 활성화 후 첫 검증
│  ├─ collect.py            # 연령×기간 수집 → data/raw/*.json
│  ├─ build_metrics.py      # 행렬 → 지표 → data/processed/books.parquet / csv
│  ├─ enrich.py             # 상위 N권 책소개·키워드·함께 빌린 책
│  └─ export_site.py        # site/public/data/*.json
├─ data/raw/ (git 제외)  data/processed/
└─ site/                    # 정적 사이트 (Astro 또는 Next.js static export)
```

- **언어**: Python 3.14 (`requests`, `pandas`, `pyarrow`) — 설치 필요.
- **호출 예산**(IP 등록 후 30,000/일 기준): 9연령 × 최대 10페이지(pageSize 500) = 90회/기간. 3개 기간 + 무필터 + 부모 랭킹 다 해도 500회 미만. 보강은 책당 1~2회 × 2,000권 ≈ 4,000회. **하루 안에 전부 가능.**
- **사이트**: 데이터가 월 단위로만 바뀌므로 정적 생성 + GitHub Pages/Vercel. 연령 탭(0~7세) × [인기/적합] 두 뷰, 책 상세, 그림책 필터, 지역 필터(2차).
- **갱신**: 월 1회 `collect → build → export` 를 스케줄 실행.

---

## 4. 단계별 진행

| 단계 | 내용 | 상태 |
|---|---|---|
| **0. 준비** | 정보나루 인증키 관리자 승인 대기, 마이페이지 **호출서버 IP 등록**, `python -m pip install requests pandas pyarrow` | ⏳ 키 `vitalizationErr`(미활성) 확인 2026-09-09 |
| **1. 스모크 테스트** | `scripts/smoke_test.py`: from_age=to_age 단일 연령이 실제로 다른 결과를 주는지, pageSize 상한, 5,000건 상한, addCode 동작 | 스크립트 준비됨, 키 활성화 대기 |
| **2. 1차 수집·계산** | 전체기간+최근12개월, 0~8세, addCode=7 → 행렬 → 지표 → CSV. **2세용 목록을 가장 먼저 뽑아 본다** | `collect.py`, `build_metrics.py` 작성·합성데이터 검증 완료 |
| **3. 검증·튜닝** | 2-5 검증, shrinkage m·밴드 임계값 조정, 무필터/부모 랭킹 비교 | |
| **4. 보강** | 책소개·키워드·함께 빌린 책·표지 | |
| **5. 사이트** | 정적 사이트 구축·배포 | Astro 골격 완성(`site/`), 합성 샘플로 빌드 확인. `export_site.py` 작성 |
| **6. 운영** | 월간 갱신, 지역 필터, 소장/대출가능 조회 | |

---

## 5. 결정 사항 (2026-09-09 확정)
1. **범위: 전국, 무분할 수집만 사용.** (2026-09-09 확정) 지역 분할은 5,000행 상한 우회용 기법이었으나 사용자 결정으로 제외. 지역 필터 기능도 계획에서 제외.
2. **기간: 전체 기간**(정보나루 집계 시작 2014-01-01 ~ 조회일 전날)을 기본 행렬로 쓴다. 추이·최신성 확인용으로 최근 12개월과 연도별 슬라이스를 추가 수집한다.
3. **연령: 0~8세 수집**, 사이트 노출은 0~7세.
4. **사이트: Astro 정적 사이트** (GitHub Pages 배포).
5. **호출서버 IP 등록 없이 우선 진행.** 한도가 낮게 걸리면 그때 등록(가정용 유동 IP 이슈 포함) 검토.

---

## 6. 키 활성화 후 실행 순서 (한 번에)

```bash
python scripts/smoke_test.py                       # 1. 단일 연령 조회 검증
python scripts/collect.py --slices allonly                  # 2. 전체기간 전국 무분할 수집 (14회)
python scripts/collect.py --slices allonly --ages 1-8 --tag kdc --cells "kdc=0,kdc=1,kdc=2,kdc=3,kdc=4,kdc=5,kdc=6,kdc=7,kdc=8,kdc=9,dtl_kdc=81,dtl_kdc=84,dtl_kdc=37,dtl_kdc=83,dtl_kdc=40,dtl_kdc=80,dtl_kdc=86,dtl_kdc=91,dtl_kdc=41,dtl_kdc=85,dtl_kdc=71,dtl_kdc=49,dtl_kdc=90,dtl_kdc=32,dtl_kdc=51,dtl_kdc=99"
#    ↑ 2b. 분야별 보충 수집 (사용자 결정 2026-09-09): 대주제 0~9 + 세부주제 상위 16개 × 1~8세 = 208회. 도서 속성이라 합집합으로 병합해 1~8세 결측을 채움
python scripts/collect.py --slices all --parents             # 2c. 연도별·최근12개월 추이 + 부모 랭킹
python scripts/collect.py --no-kids-filter --ages 0-3   #    오염 비교용(선택)
python scripts/build_metrics.py --slice all                 # 3. 지표 계산 (data/processed/loans_long.parquet)
python scripts/export_site.py                      # 4. 사이트 데이터 내보내기
cd site && npm run build                           # 5. 정적 사이트 빌드 (site/dist)
```

---

## 7. 도서관 필터 계획 (2026-09-10 제안)

### 7-1. 실측한 API 특성
| API | 용도 | 실측 |
|---|---|---|
| `libSrchByBook` (도서 소장 도서관) | 책 → 소장 도서관 | **region 필수**. 책×지역 1회로 전부 반환(수박 수영장: 서울 323곳, 경기 364곳, 0.9초). 작은도서관 포함 |
| `itemSrch` (도서관별 장서) | 도서관 → 전체 장서 | pageSize 최대 500(1000부터 빈 응답). 서울시립어린이도서관 243,755권 = 488회. `startDt/endDt`(등록일)로 신착만 증분 가능. isbn13 5개까지 지정해 소장 확인도 가능 |
| `libSrch` (도서관 목록) | 도서관 디렉터리 | 참여 도서관 **1,619곳**(경기 398, 서울 359…), 좌표·주소·홈페이지·운영시간 포함, 1회 |
| `bookExist` | 도서관×책 실시간 대출 가능 | 정적 사이트에서는 키 노출 때문에 직접 호출 불가 → 프록시 필요 |

### 7-2. 설계 (하이브리드)
1. **디렉터리**: libSrch 1회 → `public/data/libs.json` (코드·이름·주소·좌표·지역/세부지역). 피커: 지역 → 도서관(검색) 또는 브라우저 위치로 "내 주변".
2. **핵심 도서 소장 지도(책→도서관)**: 나이별 목록 노출 도서(1,212권)에 대해 libSrchByBook 을 지역별로 호출. 책→libCode 배열은 상세 조각에, 역색인 `public/data/libs/{libCode}.json`(ISBN 배열, 수 KB)은 필터 시 1개만 로드. 월 1회 신규 진입 책만 증분.
3. **내 도서관 전체 장서(도서관→책)**: 사용자가 지정한 소수 도서관은 itemSrch 로 전체 덤프(도서관당 ≤500회) → 101,790권 전체와 매칭해 풀·검색까지 커버. 이후엔 등록일 증분.
4. **실시간 대출 가능**(선택): Cloudflare Worker 등 프록시에서 bookExist 호출(키 숨김, 워커 IP 등록 필요). 1차는 도서관 홈페이지 링크로 대체.

### 7-3. UI
- 필터 패널 "도서관" 그룹: 지역 select → 도서관 select / "내 주변" 버튼. 선택은 localStorage 에 저장해 나이·검색·개월 수 페이지에 공통 적용. 카드에 "소장" 배지, 결과 상단에 "소장 정보 있는 N권 중" 표기.
- 책 상세: "소장 도서관" 섹션(지역별 개수, 가까운 순, 홈페이지 링크).

### 7-4. 호출 예산
| 단계 | 호출 | 비고 |
|---|---|---|
| 0 디렉터리 | 1 | |
| 1 핵심 도서 × 우선 지역 2곳 | 1,212×2 = 2,424 (~40분) | 사용자 지역부터 |
| 2 핵심 도서 × 나머지 15지역 | 18,180 | IP 등록 시 하루, 미등록 시 분할 |
| 3 내 도서관 전체 덤프 | 도서관당 ≤500 | 소수 도서관 |
| (풀 8,000권 전 지역) | 136,000 | 비현실적 → 3단계로 대체 |

한계: 소장 = ISBN 기준(판이 다르면 불일치), 정보나루 참여관만, 수집 시점 기준(대출 가능 여부는 별도).

### 7-5. 결정 (2026-09-10)
- 우선 지역 **부산(21)·경남(38)**. 전체 장서 덤프는 생략. 실시간 대출 가능 여부는 도서관 홈페이지 링크만.
- 호출서버 IP: 공인 IP **118.38.221.4** 를 마이페이지에 등록 (가정용 회선이라 변동 가능).
- 구현: `scripts/collect_holdings.py`(디렉터리 17회 + 핵심 도서 1,212권 × 지역, 캐시로 증분) → `export_site.export_holdings`(libs.json, libs/{code}.json, 카드 `libs`) → 나이 페이지 "도서관" 필터(지역→도서관, 내 주변), 검색·개월 수 페이지 공통 적용(localStorage), 상세 "소장 도서관" 섹션(지역별, 가까운 순, 홈페이지 링크).

### 7-6. 부산 소장 정보 전면 확대 (2026-09-10)
- 대상: 나이별 색인 합집합 **59,970권** × 부산(21). 이미 수집한 1,212권 제외하고 58,758회.
- 방식 비교: `itemSrch` 도서관 장서 덤프는 21,469회로 호출은 적지만 **우리 도서와 매칭률 1%**(전체 장서 대부분이 성인서)라 5.8GB 를 받아 1%만 쓰는 셈 → 기각.
  `libSrchByBook` 책×지역 방식을 **동시 4개 병렬**로 (건당 0.82s → 0.23s, 약 3.8시간). 추천도 상위부터 수집해 중간에 멈춰도 상위권부터 채워짐. 2,000회마다 저장하므로 재실행이 곧 재개.
- 결과 규모(추정): 책당 평균 소장 18.3곳 → 약 107만 쌍. 지역별 커버리지가 달라져(부산 전체 / 경남 핵심 1,212권) `meta.holdings.by_region` 으로 화면에 표기.
